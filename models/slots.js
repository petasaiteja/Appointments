class Slots {
    constructor(id, timestamp, date, slotsBooked , duration) {
            this.id = id;
            this.timestamp = timestamp;
            this.date = date;
            this.slotsBooked = slotsBooked;
            this.duration = duration;
    }
}

module.exports = Slots;